{
  "name": "tsumugi-storefront",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "description": "TSUMUGI — vintage & archive clothing storefront and admin console. Production build tooling for GitHub Pages.",
  "engines": {
    "node": ">=22.0.0",
    "npm": ">=10.2.0"
  },
  "scripts": {
    "build:dc": "node build/compile-dc.mjs",
    "build:vendor": "node build/vendor.mjs",
    "build:bundle": "node build/bundle.mjs",
    "prerender": "node build/prerender.mjs --out dist",
    "app": "node build/build-app.mjs",
    "verify": "node build/verify-dist.mjs dist",
    "manifest": "node build/write-artifact-manifest.mjs",
    "export:live": "node build/export-live-catalog.mjs",
    "setup:local": "node scripts/configure-local.mjs",
    "build:connected": "node scripts/build-connected.mjs",
    "cms:export-demo": "node scripts/export-demo-data.mjs",
    "cms:import-demo": "node scripts/import-demo-to-supabase.mjs",
    "cms:backup": "node scripts/export-cms-backup.mjs",
    "test": "node --test build/*.test.mjs",
    "build": "npm run build:dc && npm run test && npm run build:vendor && npm run app && npm run build:bundle && npm run verify",
    "build:bundled": "npm run build",
    "test:rls": "psql \"$DATABASE_URL\" -f supabase/tests/rls_matrix.sql"
  },
  "dependencies": {
    "@supabase/supabase-js": "2.109.0",
    "react": "18.3.1",
    "react-dom": "18.3.1"
  },
  "devDependencies": {
    "esbuild": "0.28.2",
    "parse5": "7.1.2",
    "supabase": "2.116.0"
  },
  "//": [
    "EXACT VERSIONS ONLY. A caret or a floating major means npm — or a CDN —",
    "decides what ships, and a breaking or compromised release lands in",
    "production with no commit. .npmrc pins save-exact=true so `npm i` cannot",
    "reintroduce a range.",
    "",
    "WHAT EACH DEPENDENCY IS FOR",
    "  react, react-dom            the components compiled out of the .dc.html",
    "                              sources are React components; build/vendor.mjs",
    "                              copies these two into vendor/ so the browser",
    "                              loads no CDN script.",
    "  @supabase/supabase-js       auth + Edge Function calls, previously imported",
    "                              at runtime from esm.sh. Now vendored.",
    "  parse5                      HTML parser for build/compile-dc.mjs. Build-time",
    "                              only; it never ships.",
    "  esbuild                     production bundler for the app entries and",
    "                              the CSP-safe Supabase browser build.",
    "",
    "package-lock.json is generated by npm from the exact versions above.",
    "Use `npm ci` for strict installs and `npm run build` for the verified,",
    "fully bundled GitHub Pages artifact."
  ]
}
