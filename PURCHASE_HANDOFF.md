# 一点物の在庫・販売状態

## 現在の公開サイト

ポートフォリオとして購入受付は無効です。購入ボタンや決済サービスを有効にする変更は今回行っていません。
注文APIも `COMMERCE_ENABLED=true` をサーバー側で明示設定するまでは `403 commerce_disabled` を返します。現時点でこの設定は有効化しません。

## 実装済みのサーバー処理

既存の Supabase `public.create_order` は、注文作成と在庫減算を一つのトランザクションで行います。
今回、サーバー専用の実行権限の不足を修正しました。顧客・管理者のブラウザからこの処理を直接実行する権限はありません。

- 注文確定で数量を減算。
- 残数0で商品状態を `soldout`（売り切れ）へ変更。
- 残数があれば公開状態を維持。
- 残数不足・売り切れ・注文途中の失敗では、注文全体を取り消す。
- 決済状態は `pending`。注文作成を入金完了とは扱わない。

`supabase/tests/one_of_a_kind.sql` がこの動作を実データベースで検証します。
テスト用の商品・注文はロールバックされますが、注文番号には採番による欠番が生じます。

## 実購入を有効にする前に必要な別工程

決済事業者の選定・署名検証付き決済通知・重複通知への対応・未入金注文の期限と在庫解放・キャンセル/返品時の在庫復帰方針・公開ページの在庫反映を接続する必要があります。
購入完了画面への遷移だけで決済済みにしたり、在庫を再度減らしたりしないでください。
現在の処理は注文時に在庫を確保する方式です。決済成功時にもう一度減算すると二重減算になります。

## 通知

在庫1点や正しく売り切れになった商品は警告しません。
公開商品の在庫・メイン画像・必須情報の不備、掲載有効な注目コンテンツの参照切れを要確認として表示します。
正常な在庫数・売り切れ商品数は通常の集計で確認します。

## セキュリティ確認

変更後の診断で、注文控え・レート制限・セキュリティ設定テーブルの「RLS有効・ポリシーなし」は、ブラウザからの直接利用を拒否する意図どおりの情報通知です。
既存の「漏えいパスワード保護が無効」という警告は残っています。今回の作業では認証プランや有料機能を変更していません。
[Supabase公式：パスワード保護](https://supabase.com/docs/guides/auth/password-security#password-strength-and-leaked-password-protection)
